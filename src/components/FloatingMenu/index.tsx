import React from 'react';
import { StyledFloatingMenu } from './styled';

const FloatingMenu: React.FC = () => {
  return (
    <StyledFloatingMenu>
    </StyledFloatingMenu>
  );
};

export default FloatingMenu;
